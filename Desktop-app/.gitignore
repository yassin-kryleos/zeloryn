# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-backend
dist-desktop
dist-ssr
*.vsix
*.local

# Environment and secrets (SEC-B2) — never commit real .env
.env
.env.*
!.env.example
.kryleos_fallback.key
.kryleos_db.key

# Runtime / local app state
chat_history.json
.kryleos/
.kryleos_forge_secrets.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# QA test artifacts
chat_history.test.json
chat_history.performance.json
qa-db/
qa-db-performance/
playwright-report/
test-results/

# Packaged e2e scratch dir (packaged.e2e.ts / playwright.packaged.config.ts)
# - contains runtime secrets/keys copied for the packaged app sandbox
.tmp-e2e/
