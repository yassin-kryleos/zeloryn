# Launch Assets

- `01-no-key-onboarding.png`: real desktop onboarding state.
- `02-demo-trace.png`: real deterministic green trace, captured from the running app.
- `trace-demo.gif`: short launch loop assembled from the real screenshots.
- `companion-approval.gif`: companion approval narrative asset. Product behavior is separately covered by companion E2E tests.

The GIFs are launch summaries, not substitutes for runtime tests.
